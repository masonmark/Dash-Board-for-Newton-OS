• RECENT APP TYRACKING SHIZNITZOID///AVI's Recent apps patch from originator's hierarchy array.ViewFinder._proto._proto.newButtonToggle RO frame:_function #6 Ref(-536736307)func() begin  local Local1, Local2, Local3;  Local1 := GetRoot().|Avi's Backdrop:AviD|.recentApps;  Local2 := self.appSymbol;  if Local2 <> nil and Local2 <> 'copperfield and Local2 <> 'extrasDrawer    then begin      for Local3 := length(Local1) - 1 to 0 by -1 do if Local1[Local3].symbol = Local2        then ArrayRemoveCount(Local1, Local3, 1);      AddArraySlot(Local1, {symbol: Local2, Time: TimeInSeconds()});      ArrayRemoveCount(Local1, 0, max(0, length(Local1) - GetRoot().|Avi's Backdrop:AviD|.prefs.howManyRecentApps));    end;  inherited:?buttonToggle()endGetRoot().buttonToggle#448BB5   <function, 0 arg(s) #448BB5>GetRoot().buttonToggle := func()	begin		PRINT("BUTTONTOGGLE_CALLED! here is self:");  //self iis the app tapped, apparently! woo!		PRINT(SELF);		inherited:?buttonToggle();	end;