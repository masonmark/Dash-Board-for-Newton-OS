 RO frame:_function #5 Ref(-527188907)External objects used: • kObject1: {#2} • kObject2: {#2} • kObject3: {#2} • kObject4: {#2}func(Arg1) begin  local Local1, Local2, Local3, Local4, Local5, Local6, Local7, Local8;  InkOff(Arg1);  if Einstellungen.viewCObject    then begin      p := GetAppParams().appAreaBounds;      if p.right - p.left < p.bottom - p.top and Einstellungen.einstell.Hoch = 'Fr or (p.right - p.left >= p.bottom - p.top and Einstellungen.einstell.Quer = 'Fr)        then begin          :Drag(Arg1, nil);          :MoveBehind(nil);          return true;        end;    end;  :hilite(true);  Clicker();  Local1 := [];  Local2 := [];  AddArraySlot(Local1, nil);  AddArraySlot(Local1, nil);  AddArraySlot(Local2, {item: "Tasklist Settings..."});  AddArraySlot(Local2, {item: 'pickSeparator});  foreach Local7, Local8 in GetRoot() do if IsFrame(Local8) and Local8.viewCObject and Local8.viewFlags and band(Local8.viewFlags, 5) = 5 and Local7 <> 'root    then (if einstell.mitBildern      then begin        for Local6 := 0 to length(ExtraInfos) - 1 do if ExtraInfos[Local6].slot = Local7          then break;        if Local6 < length(ExtraInfos)          then (Local4 := ExtraInfos[Local6].infoFrame);          else begin            Local4 := extrasDrawer:SetExtrasInfo(Local7, {});            AddArraySlot(ExtraInfos, {slot: Local7, infoFrame: Local4});          end;        if Local4          then (Local3 := Local4.text);          else (Local3 := GetAppName(Local7));        if StrLen(Local3) = 0          then (Local3 := SPrintObject(Local7));        if Local7 = 'findDrawer          then (Local3 := "Suchen");        for Local6 := 2 to length(Local2) - 1 do if StrCompare(Local3, Local2[Local6].item) < 0          then break;        ArrayInsert(Local1, Local8, Local6);        if IsFrame(Local4) and Local4.icon.icon          then (Local4 := Local4.icon);        if IsFrame(Local4) and Local4.icon          then (Local5 := Local4.icon);          else (if Local7 = 'cardFile            then (Local5 := "kObject1");            else (if Local7 = 'calendar              then (Local5 := "kObject2");              else (if Local7 = 'extrasDrawer                then (Local5 := "kObject3");                else (if Local7 = 'findDrawer                  then (Local5 := "kObject4")))));        ArrayInsert(Local2, {item: Local3, icon: Local5, indent: 40}, Local6);      end;      else begin        Local3 := GetAppName(Local7);        if StrLen(Local3) = 0          then (Local3 := SPrintObject(Local7));        if Local7 = 'findDrawer          then (Local3 := "Search");        AddArraySlot(Local2, Local3);        AddArraySlot(Local1, Local8);      end);  Symbole := Local1;  :PopupMenu(Local2, nil);  :hilite(nil);  trueend//KOBJECT1-4 are icons