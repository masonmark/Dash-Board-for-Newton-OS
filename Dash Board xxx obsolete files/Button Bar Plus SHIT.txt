foreach i in GetStores() do	begin		local bbpRef := GetPkgRef("ButtonBar Plus:SAS", i);		if bbpRef then break;	end;	if bbpRef and IsPackageActive(bbpRef) then	begin		print("BUSTER B-I-ITCHES ON THE PREMISES!");		if ModalConfirm("The package \"Button Bar Plus\" is currently installed. This package is incompatible with Dash Board. Dash Board cannot run until this package is frozen or removed." &&		"Would you like to freeze it now?", 'yesNo) then			SafeFreezePackage(bbpRef);		else			Print("boo dnag canelled");