GetRoot().buttontoggle frame:_function #6 Ref(205630325)External objects used: • kFunc1: newfunc(#0)func() begin  local Local1, Local2, Local3;  if band(self.viewFlags, 64) = 64    then return inherited:ButtonToggle();  Local1 := not Visible(self);  Local2 := call "kFunc1" with ();  if :?buttonToggleScript(Local2)    then return Local1;  if Visible(self) and Local2 <> self    then begin      Local3 := GetRoot().(GetUserConfig('blessedapp));      if self <> Local3        then :MoveBehind(nil);        else begin          fronView := GetView('viewfrontmost);          if Local2            then begin              :SysBeep();              Local2:Close();            end;        end;      nil    end;    else begin      :Toggle();      Local1    endendGetRoot().buttontoggle.kFunc1 frame:_function #5 Ref(205630181)func() begin  local Local1, Local2;  foreach Local2 in GetRoot():ChildViewFrames() do if band(Local2.viewFlags, 64) = 0    then (Local1 := Local2);  Local1end